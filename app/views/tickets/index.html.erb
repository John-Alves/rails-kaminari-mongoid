<p id="notice"><%= notice %></p>

<h1>Tickets</h1>

<table>
  <thead>
    <tr>
      <th>Label</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tickets.each do |ticket| %>
      <tr>
        <td><%= ticket.label %></td>
        <td><%= link_to 'Prices', product_ticket_prices_path(@product, ticket) %></td>
        <td><%= link_to 'Edit', edit_product_ticket_path(@product, ticket) %></td>
<!--        <td><%#= link_to 'Destroy', product_ticket_price_path(@product, ticket), method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back', products_path %>
<%= link_to 'New Ticket', new_product_ticket_path(@product) %>
